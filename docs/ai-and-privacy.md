# AI and Privacy

AI is optional and uses OpenRouter when configured.

Before an AI request, review the context preview. Metadata-only mode is the default. Raw sample rows are opt-in and should be used only after checking the fields that will be sent.

OpenRouter API keys are stored locally and are excluded from workspaces, exports, logs, debug reports, and support bundles.

AI output is treated as untrusted input.
