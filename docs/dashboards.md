# Dashboards

Dashboards combine saved visualizations as cards.

Use dashboards to:

- Add saved visualizations
- Refresh cards
- Apply compatible filters
- Use linked filtering where configured
- Export dashboard packages or snapshots

Linked filtering applies only to targets with compatible fields or parameters.
