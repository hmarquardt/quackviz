# Maps

Maps use MapLibre GL JS for point and region-based visualization.

Imported data remains local. Remote basemap providers receive tile requests, not imported datasets.

Region names are matched conservatively. QuackViz does not silently fuzzy-match regions; approved mappings are explicit.

Map image export may be limited by third-party tile-server CORS policy.
