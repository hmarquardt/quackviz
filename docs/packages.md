# Export and Backup

QuackViz can export local backups, standalone apps, dashboard packages, report packages, visualization packages, and snapshot-style artifacts.

Packages never include OpenRouter API keys. Data inclusion is explicit and should be reviewed before export.

Integrity hashes detect changed files but do not verify publisher identity.
