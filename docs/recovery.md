# Recovery

Use More for workspace validation, recovery checkpoints, safe mode, and support bundles.

Safe mode can also be opened with `?safeMode=1`. Safe mode does not delete or rewrite workspace data automatically.

Support bundles exclude API keys and source data by default.
