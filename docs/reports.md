# Reports

Reports arrange text, findings, tables, visualizations, dashboard snapshots, and methodology sections into a narrative.

Exports contain snapshots unless sections are refreshed. Refresh stale dynamic sections before exporting if source data or saved visualizations changed.

Save as PDF uses the browser print system.
