# Writing SQL

QuackViz runs DuckDB SQL locally in the browser.

Use starter queries from the Analyze tab for:

- Preview rows
- Row counts
- Null counts
- Numeric summaries

Review generated SQL before running it. AI-generated SQL is treated as untrusted input and is never executed silently.
