# Visualizations

Run a query first, then open Visualize to choose chart settings.

Supported chart families include trend, comparison, and map-backed visualizations. The builder remains the canonical editing surface; generated or recommended settings must still be saved explicitly.

Save a visualization before adding it to a dashboard or report.
